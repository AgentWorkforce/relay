{
  "mcpServers": {
    "relaycast": {
      "args": [
        "-y",
        "@relaycast/mcp"
      ],
      "command": "npx",
      "env": {
        "RELAY_AGENT_NAME": "broker",
        "RELAY_API_KEY": "${RELAY_API_KEY}",
        "RELAY_BASE_URL": "https://api.relaycast.dev"
      }
    }
  }
}
