{
  "name": "@agent-relay/telemetry",
  "version": "8.8.0",
  "description": "Deprecated Agent Relay telemetry package placeholder",
  "private": true,
  "deprecated": "@agent-relay/telemetry is deprecated. Telemetry is now internal to the agent-relay CLI.",
  "type": "module",
  "scripts": {
    "build": "echo '@agent-relay/telemetry is deprecated; telemetry now lives inside the agent-relay CLI.'",
    "clean": "rm -rf dist",
    "test": "echo '@agent-relay/telemetry is deprecated; no package tests to run.'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AgentWorkforce/relay.git",
    "directory": "packages/telemetry"
  }
}
