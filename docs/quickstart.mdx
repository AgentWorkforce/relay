---
title: 'Quickstart'
description: 'Spawn your first agents and send messages between them.'
---

## Install

```bash
npm install @agent-relay/sdk
```

## Spawn Agents and Send a Message

```typescript
import { AgentRelay, Models } from '@agent-relay/sdk';

const relay = new AgentRelay();

// Spawn a planner (Claude) and a coder (Codex)
const planner = await relay.claude.spawn({
  name: 'Planner',
  model: Models.Claude.OPUS
});

const coder = await relay.codex.spawn({
  name: 'Coder',
  model: Models.Codex.GPT_5_3_CODEX
});

// Listen for messages
relay.onMessageReceived = (msg) => {
  console.log(`${msg.from} → ${msg.to}: ${msg.text}`);
};

// Send a message from Planner to Coder
await planner.sendMessage({ to: 'Coder', text: 'Implement the auth module' });

await relay.shutdown();
```

## Supported CLIs

| CLI    | Constant prefix        |
| ------ | ---------------------- |
| Claude | `Models.Claude.*`      |
| Codex  | `Models.Codex.*`       |

## Next Steps

<CardGroup cols={2}>
  <Card title="TypeScript SDK" icon="js" href="/workflows/typescript-sdk">
    Full TypeScript workflow examples with multi-agent patterns.
  </Card>
  <Card title="Python SDK" icon="python" href="/workflows/python-sdk">
    Python workflow examples.
  </Card>
  <Card title="SDK Reference" icon="book" href="/reference/sdk">
    Complete API reference.
  </Card>
  <Card title="Electron Integration" icon="window" href="/guides/electron-integration">
    Use the SDK inside an Electron app.
  </Card>
</CardGroup>
