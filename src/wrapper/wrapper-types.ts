/**
 * @deprecated Import from '@agent-relay/wrapper' instead.
 */
export * from '@agent-relay/wrapper';
export type {
  InjectionFailedEvent,
  SummaryEvent,
  SessionEndEvent,
  AuthRevokedEvent,
} from '@agent-relay/wrapper';
