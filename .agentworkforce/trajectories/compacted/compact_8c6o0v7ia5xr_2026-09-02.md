# Trajectory Compaction: 2026-09-02 - 2026-09-02

## Summary
A single 43-minute session (commit `0fe170b49`, branch `fix/claude-injection-submit-0902`) replaced the Windows PTY write-admission ordering path in `crates/relay-pty/src/pty.rs`. The prior design used `CancelSynchronousIo` to interrupt an in-flight blocking `ReadFile` so a verified-write admission could linearize against the reader; Windows CI proved that cancellation can hang indefinitely on an idle ConPTY read. Because `portable-pty` 0.8.1 erases the Windows reader handle behind a boxed `Read` and exposes no raw handle or readiness API, no fix was possible from Relay's side alone — the reader could never wake while idle, and atomics cannot linearize a syscall that has not returned. The chosen fix vendors the exact crates.io release of `portable-pty` 0.8.1 (upstream commit `4afedd626`, checksum recorded in `vendor/portable-pty/README.relay.md`) under `vendor/portable-pty/`, excluded from workspace membership and wired in via a root `[patch]` in `Cargo.toml`. The Relay delta is deliberately one channel wide: the Windows ConPTY *output* pipe becomes a private, UUID-named, local-only, non-inheritable byte pipe whose read end is opened `PIPE_NOWAIT`, and the reader surfaces `ERROR_NO_DATA` as `io::ErrorKind::WouldBlock`. ConPTY input keeps upstream's blocking anonymous `CreatePipe`. `uuid` is the only added dependency. A bare `PeekNamedPipe` readiness probe was evaluated and rejected — Microsoft documents that it can itself block on a synchronous handle in a multithreaded process. On the Relay side, `pty.rs` now does an immediate physical read and assigns the producer sequence under the `output_order: Arc<Mutex<()>>` guard (`read_and_tag` at ~pty.rs:676, `let _order_guard = output_order.lock()` then `output_sequence.fetch_add`), idles on `WouldBlock` in the reader loop (~pty.rs:881), and keeps VT/UTF-8 parsing and bounded chunk publication *outside* the lock. Queue admission snapshots the boundary sequence under the same lock, so output tagged `<= boundary` is provably pre-admission (stale) and `> boundary` is fresh. Coverage added: a Windows-only primitive regression inside the vendor (idle prompt reads, cloned reader, round-trip, EOF, handle non-inheritability), a bounded live-ConPTY proof emitting `relay-pty-live-proof stage=stale-output|fresh-output`, a no-late-write test, and a focused Windows lane in `.github/workflows/rust-ci.yml`. Green at session close: relay-pty 230 native tests, broker 1037 + 16 integration, Clippy (`--no-deps`), fmt, actionlint, diff checks, Windows MSVC check and cross-Clippy, plus targeted vendor cross-Clippy. Exact Windows *runtime* CI remains the outstanding external proof.

## Key Decisions (3)

| Question | Decision | Impact |
|----------|----------|--------|
| How to wake an idle ConPTY reader so verified write admission can be ordered against physical reads, given `CancelSynchronousIo` blocks? | Vendor `portable-pty` 0.8.1 verbatim under `vendor/portable-pty/` and change only the Windows ConPTY output pipe to a private UUID-named non-inheritable byte pipe opened with `PIPE_NOWAIT`; preserve `ERROR_NO_DATA` as `WouldBlock`. Apply via a root `[patch]`; keep the vendor out of workspace membership. | Adds ~3000 lines of vendored third-party source and a `uuid` dependency, but keeps the Relay-authored delta to one pipe. Input path behavior is unchanged. Any future `portable-pty` upgrade must re-apply the ConPTY output-pipe patch; `README.relay.md` is the delta's only documentation. |
| How to lint a vendored path dependency without either hiding Relay's new code or drowning in upstream warnings? | Run workspace Clippy with `--no-deps`, and separately Windows-cross-Clippy the vendored ConPTY code allowing only enumerated upstream lint names. | `--no-deps` still lints both selected default workspace packages (broker, relay-pty) at `-D warnings`; the new vendor code stays covered by its own targeted, narrowly-allowlisted pass. |
| Where does sequence tagging happen relative to the ordering mutex? | Hold `output_order` across exactly the physical `ReadFile` + `output_sequence.fetch_add`; do VT/UTF-8 parsing and bounded chunk send outside the lock. | Admission stays bounded and fail-closed; `OutputChunk::sequence()` is a sound stale/fresh discriminator at `<= boundary` / `> boundary`. |

## Conventions Established
- **Third-party patches live as a vendored exact crates.io release under `vendor/<crate>/`, excluded from workspace members, applied by a root `[patch]`, with a `README.relay.md` recording upstream commit, crates.io checksum, and an explicit "Relay delta" section; `Cargo.toml.orig`, `.cargo_vcs_info.json`, and `LICENSE.md` are retained unmodified.**: Keeps the diff against upstream auditable, avoids a second repository/merge dependency, and preserves license provenance. (scope: Any future native-dependency patch in the relay monorepo.)
- **Vendored source is never blanket-`allow`ed; workspace Clippy runs `--no-deps` and the vendored delta gets its own targeted cross-target Clippy pass with only enumerated upstream lints allowed.**: Blanket allows would silence diagnostics in newly authored code inside the vendor. (scope: CI lint configuration (`.github/workflows/rust-ci.yml`) for any vendored crate.)
- **Concurrency invariants get a primitive-level regression test *plus* a bounded live-ConPTY proof that prints machine-greppable markers (`relay-pty-live-proof stage=stale-output sequence=… boundary=…`), and the platform-specific case gets its own CI lane rather than riding the generic matrix.**: A cross-compile check proves the code builds; only a runtime lane on the actual OS proves the syscall semantics. The markers make the ordering claim verifiable from CI logs instead of inferred from a green check. (scope: `crates/relay-pty` tests and Windows CI lane; applies to any OS-specific blocking-IO work.)
- **Scope OS-specific pipe changes to a single direction — patch the ConPTY *output* channel only, leave input on upstream blocking semantics.**: Minimizes divergence surface and keeps the child-process-facing write path identical to upstream. (scope: `vendor/portable-pty/src/win/conpty.rs`, `win/mod.rs`.)

## Lessons Learned
- `CancelSynchronousIo` is not a reliable wakeup for an idle ConPTY `ReadFile` — it can block indefinitely, which surfaced only in Windows CI, not in cross-compile or lint gates. (The pre-existing write-admission ordering path depended on cancelling an in-flight blocking read to establish a happens-before edge.) - For Windows blocking-IO ordering, make the handle nonblocking at creation (`PIPE_NOWAIT` on a named byte pipe) and poll on `WouldBlock`, rather than trying to interrupt a synchronous syscall after the fact.
- An abstraction that erases the underlying OS handle can make a correctness fix impossible in the consumer crate, no matter how much synchronization you add above it. (`portable-pty` 0.8.1 (and latest upstream) box the Windows reader as `Read` with no raw handle or readiness accessor, so no atomic or mutex in `pty.rs` could linearize a syscall return.) - When an ordering bug traces to a dependency's erased handle, evaluate vendor-vs-fork early instead of iterating on consumer-side synchronization; the smallest sound fix may be one line below the abstraction boundary.
- `PeekNamedPipe` is not a safe readiness probe — it can itself block on a synchronous handle in a multithreaded process. (It was the first candidate design (readiness probe on a duplicated ConPTY output handle) before being rejected on documented behavior.) - Read the MSDN blocking caveats before treating any Win32 "peek/query" call as nonblocking; prefer a handle that is nonblocking by construction.
- Adding a path dependency silently expands lint scope to third-party code and can turn a green pipeline red for reasons unrelated to the change. (Eleven legacy Clippy warnings from unchanged 0.8.1 source appeared under Rust 1.94 once the vendor became a path dep.) - Pair any vendoring with an explicit lint-scope decision (`--no-deps` + a separate targeted vendor pass) in the same PR.
- Cross-compile check + cross-Clippy on Windows targets prove compilation, not runtime semantics; the fix's core claim remained unproven at session close. (Native suites (relay-pty 230, broker 1037 + 16 integration), fmt, actionlint, and Windows MSVC check were all green while the actual Windows runtime lane had not executed.) - Treat "Windows runtime CI green" as the completion gate for this change and do not report the ConPTY ordering as verified until that lane runs.

## Open Questions
- Exact Windows runtime CI has not yet run — the `PIPE_NOWAIT` / `ERROR_NO_DATA`→`WouldBlock` ordering behavior is still unproven on real hardware; this is the outstanding external proof for the whole change.
- Should the `PIPE_NOWAIT` ConPTY output-pipe change be proposed upstream to `portable-pty` so the vendor can eventually be dropped, or is the vendor intended to be permanent?
- What is the maintenance plan for re-applying the ConPTY output-pipe delta on a future `portable-pty` upgrade (no automated drift check exists — only the prose in `vendor/portable-pty/README.relay.md`)?
- Is the ~3000-line vendor plus the new `uuid` dependency acceptable to reviewers, and does the vendored source need to be excluded from any repo-wide security/dependabot or coverage tooling?
- Does the `PIPE_NOWAIT` busy-poll reader loop have a measurable CPU cost on idle Windows PTYs, and does it need a backoff?
- The changelog was not touched — does this Windows PTY fix warrant an `[Unreleased - Patch]` entry in `CHANGELOG.md` per the repo's curation rule?

## Stats
- Sessions: 1, Agents: default, Files: 20, Commits: 1
- Date range: 2026-09-02T06:49:16.902Z - 2026-09-02T07:32:52.792Z
