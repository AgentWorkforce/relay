# Operator reactivation check — 2026-09-08

Disposition: still blocked on the upstream compatibility contract. No broker
implementation resumed. The existing handoff at `abb5f08` remains applicable.
The seat is releasable **from a work-handoff perspective** after this update is
pushed: no implementation or verification job needs to keep running, and the
remaining work, acceptance tests, and recovery design are saved. This does not
authorize or establish the safety of any identity-release operation; no agent
was released, including this one.

## What changed upstream

Inspected Relaycast main
`13220ab4bb6cc7f7884a7bc2b4f6e08ff693218a` (September 7, 22:55:27 UTC),
and the latest release, v8.5.2. The optional generation guard from #377 remains
present, but the compatibility prerequisite has **not** landed:

- [Release route](https://github.com/AgentWorkforce/relaycast/blob/13220ab4bb6cc7f7884a7bc2b4f6e08ff693218a/packages/engine/src/routes/agent.ts#L120)
  still accepts optional `expected_token_hash` on `/v1/agents/release`.
- [Rust guarded method](https://github.com/AgentWorkforce/relaycast/blob/13220ab4bb6cc7f7884a7bc2b4f6e08ff693218a/packages/sdk-rust/src/relay.rs#L551)
  still uses that same endpoint. The crates.io sparse index reports 8.0.0 as
  the latest non-yanked SDK; this checkout pins 8.0.0, without that method.
- The route and Rust SDK files did not change between the previously inspected
  `04fc9bc` and current main. No replacement conditional endpoint was identified.

## Deployment evidence and limits

[Deploy run 34168304309](https://github.com/AgentWorkforce/relaycast/actions/runs/34168304309)
succeeded at current main. Its per-job results were:

| Job | Result |
| --- | --- |
| Lint, Build & Test | SUCCESS |
| Deploy MCP to Smithery | SUCCESS |
| Deploy Observer Dashboard | SUCCESS |
| Deploy to Cloudflare Pages | SUCCESS |
| Deploy Observer Host Router | SUCCESS |

No job was SKIPPED in that run. However,
[the workflow](https://github.com/AgentWorkforce/relaycast/blob/13220ab4bb6cc7f7884a7bc2b4f6e08ff693218a/.github/workflows/deploy.yml)
deploys observer/marketing surfaces and publishes the MCP listing; it does not
establish the deployed API engine revision. An unauthenticated GET to
`https://cast.agentrelay.com/health` returned **403 Forbidden** from this
environment. The GitHub deployments query returned no records. Therefore the
production engine version, fleet-wide rollout, and live guard enforcement are
**unverified**, not claimed absent or deployed. No mutating endpoint was probed.

## Exact remaining dependency and resume trigger

Relaycast must supply a conditional-release request that unsupported servers
reject before any release side effect—for example, a new endpoint requiring
the original token-generation hash. A legacy server must not silently drop
the condition and release by name. The existing #377 optional field does not
satisfy that backward-compatibility property. Publish the matching Rust SDK
surface and provide API-engine deployment evidence for the new contract.
No new issue or PR tracking that narrower compatibility prerequisite was
identified in this check; #377 is the already-merged generation-CAS work.

Resume this lane when that contract is available: preserve the captured
generation in a durable pre-teardown intent; use the existing 200/400 ms retry
policy for only 500/502/503/504; retain failed/ambiguous intents for explicit
recovery; verify terminal cleanup and same-ID takeover safety. Full design and
required red/green cases remain in `findings.md`.

Relay #1672 remains OPEN/CONFLICTING at
`79d0fbf6cfea5b25658455aa76752ec7329bd205`. GraphQL reviewThreads returned
`hasNextPage=false` and the same one unresolved
[generation-guard P1](https://github.com/AgentWorkforce/relay/pull/1672#discussion_r3943190504).
It was not marked resolved. No new Relay PR was opened.

No broker restarted, identity taken over or released, merge performed, or
deployment triggered. Product files are unchanged; Rust checks were not rerun
for this handoff-only update (the previous fmt/Clippy evidence remains in the
saved audit).
