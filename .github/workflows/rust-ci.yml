name: CI

on:
  pull_request:
  push:
    branches: [main]

# Reusable workflows cannot elevate the caller's token permissions.
# detect-changes.yml reads pull-request file lists and commit comparisons.
permissions:
  contents: read
  pull-requests: read

env:
  CARGO_TERM_COLOR: always
  AGENT_RELAY_TELEMETRY_DISABLED: 1

jobs:
  changes:
    uses: ./.github/workflows/detect-changes.yml

  rust-test:
    name: Rust Tests (${{ matrix.os }})
    needs: changes
    if: needs.changes.outputs.rust_changed == 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          cache-bin: false
      - name: Run Rust tests
        run: cargo test
      - name: Run Rust tests (release)
        run: cargo test --release

  relay-pty-windows-test:
    name: Relay PTY Synchronization Tests (windows-latest)
    needs: changes
    if: needs.changes.outputs.rust_changed == 'true'
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          cache-bin: false
      - name: Nonblocking ConPTY pipe primitive
        run: cargo test --manifest-path vendor/portable-pty/Cargo.toml --lib win::conpty::tests::nonblocking_output_pipe_is_prompt_private_and_lossless -- --exact
      - name: Read/tag ordering regression
        run: cargo test -p relay-pty --lib pty::tests::completed_read_is_tagged_before_verified_write_can_sample_boundary -- --exact
      - name: Output backpressure regression
        run: cargo test -p relay-pty --lib pty::tests::output_channel_backpressure_does_not_hold_verified_write_ordering -- --exact
      - name: Live ConPTY boundary regression
        run: cargo test -p relay-pty --lib pty::tests::verified_write_boundary_includes_output_still_queued_for_consumer -- --exact --nocapture
        timeout-minutes: 2

  rust-cross-compile:
    name: Cross-compile check (${{ matrix.target }})
    needs: changes
    if: needs.changes.outputs.rust_changed == 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            linker: aarch64-linux-gnu-gcc
            packages: gcc-aarch64-linux-gnu
          - os: macos-latest
            target: x86_64-apple-darwin
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}
          cache-bin: false
      - name: Install cross-compilation tools
        if: matrix.packages
        run: |
          sudo apt-get update
          sudo apt-get install -y ${{ matrix.packages }}
      - name: Cross-compile agent-relay-broker
        run: cargo check --target ${{ matrix.target }} --bin agent-relay-broker
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: ${{ matrix.linker }}

  rust-clippy:
    name: Clippy (${{ matrix.os }})
    needs: changes
    if: needs.changes.outputs.rust_changed == 'true'
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          cache-bin: false
      - run: cargo clippy --no-deps -- -D warnings

  rust-fmt:
    name: Format
    needs: changes
    if: needs.changes.outputs.rust_changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt -- --check

  sdk-check:
    name: SDK TypeScript Check
    needs: changes
    if: needs.changes.outputs.node_changed == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: scripts/post-publish-verify/retry-command.sh "SDK npm install" npm install
      - run: npm run -w @agent-relay/sdk check
